// Drizzle schema. Intentionally empty in Phase 1.
// Tables (tenants, api_keys, provider_allowlist, usage_ledger, request_logs)
// are defined in a later phase so the persistence model can be designed in
// one focused pass instead of being bolted on incrementally.

export {};
