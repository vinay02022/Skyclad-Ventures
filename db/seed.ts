// Placeholder seed script. Real seeding (tenants, api keys, model price table)
// arrives once the schema lands. Kept executable so `npm run db:seed` works
// from day one and the README never references a missing script.

async function main(): Promise<void> {
  console.log("[seed] no schema yet — nothing to seed in Phase 1.");
}

void main();
